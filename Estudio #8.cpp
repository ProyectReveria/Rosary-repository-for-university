#include <iostream>

int main(){
    //int grade = 40;
    //grade >= 60 ? std::cout <<"pasaste":std::cout<<"reprobaste";

    int number = 8;
    // Variable ? True state :: false state.
    number % 2 == 1 ? std::cout <<"Impar" : std::cout<< "even";
    return 0;
}
